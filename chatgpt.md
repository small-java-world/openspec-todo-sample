# OpenSpecを用いた仕様運用ベストプラクティス集

## 1. OpenSpecにおける仕様・設計・実装の責務分離原則

OpenSpecでは仕様 (Spec)・設計 (Design)・実装 (Implementation)の役割を明確に分離することが重要です。プロジェクトにはopenspec/ディレクトリが作成され、各種ファイルに責務ごとの内容を記載します。例えば、現在のシステム仕様はopenspec/specs/以下の各機能フォルダにまとめられ、spec.mdに要求仕様（要件定義とシナリオ）が記述されます。また必要に応じてdesign.mdに技術的な設計・パターンを補足できます。一方、変更提案ごとのフォルダ（openspec/changes/<change-id>/）には提案内容や実装計画が含まれ、proposal.mdに変更の理由・内容・影響範囲、tasks.mdに実装タスクリスト、design.md（任意）に技術的決定事項を記載します。このように**「何を実現すべきか」は仕様書に、「どう実現するか」は設計書に、「何を行うか（手順）」**はタスクリストに分離するのが原則です。

この責務分離により、仕様書が常に最新の**単一の真実 (Single Source of Truth)**として機能し、設計上の検討や具体的な実装手順は別途管理できます。実装完了後は提案フォルダ内の差分仕様を既存のspecs/にマージし、アーカイブすることで、仕様書（spec.md）は最新のシステム像を正確に反映します。こうしたプロセスにより、要求仕様とコード実装の不整合を防ぎ、各段階でレビューすべき内容を明確にできます。

## 2. 重複を避ける構造化パターン（横断要件、参照方法、アンチパターン）

大規模なシステムでは、認証や監査ログなど横断要件が複数の機能にまたがることがあります。このような場合、同じ要件を各所に重複記載するのはアンチパターンです。OpenSpecでは変更ごとのフォルダ内で複数の機能仕様を同時に更新することが可能で、横断的な変更でも一箇所の提案にまとめて管理できます。例えば、二要素認証の追加に伴い認証機能と通知機能の両方を変更する場合、changes/add-2fa-notify/フォルダ内にそれぞれの差分spec.mdを用意し、authとnotificationsそれぞれの仕様に対する追加を記述できます。この構造により、特定の機能領域ごとの要件を分離しつつ、変更提案単位では横断的に追跡でき、記述の重複や漏れを防ぎます。「一つのspecファイルが一つの機能領域を担う」ようにし、複数領域にまたがる要件は変更提案の中で整理するのがベストプラクティスです。

また、参照方法を工夫することで重複を減らし整合性を保てます。提案書や設計書では影響範囲として関連する仕様ファイルパス（例: specs/auth/spec.md）やソースコードパス（例: src/auth/）を明示し、読者やAIに影響箇所を正確に伝えます。例えば提案書の「Impact（影響）」欄に「影響を受ける仕様: auth/spec.md」と記載すれば、該当の仕様に変更が及ぶことが一目で分かり、同じ説明を重複して書く必要がありません。実装タスクでも、変更するファイル名やクラス名をfile.ts:42のように具体的に示すことで、説明のために長い文章を書く代わりにポイントを的確に共有できます。このようにリンクやパスで既存リソースを参照し、記述を再利用することで、要件の断片的なコピー＆ペーストによる不整合を避けます。

最後に、OpenSpec運用で避けるべきアンチパターンを押さえておきましょう。以下のような行為は重複や混乱を招くため推奨されません。

要件の多重定義: 同一の要件を複数の仕様書に重複記載すること。共通要件はプロジェクト全体の規約としてproject.mdにまとめるか、影響箇所を参照で示し、重複定義は避けます。

仕様への実装詳細の埋め込み: 仕様書にデータベーススキーマやアルゴリズムの詳細実装を書いてしまうこと。これらは設計やタスクに委譲し、仕様書では「あくまで振る舞いと要件」を記述します（詳細は後述のDB項目参照）。

シナリオ欠如・不明確な仕様: 要件に対応する具体的なシナリオ（ユースケース）を書かないこと。【仕様書の各要件には最低1つのシナリオを示し、受け入れ基準を明確化します【28†L139-L145】】。シナリオのない要件定義は曖昧さを生み、AI実装も的外れになりがちです。

巨大な変更の一括提案: 関連の薄い複数の変更を1つの提案にまとめたり、一度に大規模な仕様変更を行おうとすること。変更ID（change-id）は機能単位・目的単位で分割し、一貫性のある粒度で提案する方が追跡容易です（名前も衝突しないようkebab-caseで簡潔に付けます）。

仕様と実装の不整合放置: 仕様書を更新せずにコードだけ変更したり、提案のアーカイブを怠ること。これにより「仕様ではこうだが実装が違う」という齟齬が生まれ、以降の開発効率が大きく低下します。

以上のパターンを避け、一貫した構造と参照で仕様管理することが、OpenSpecを活用する上での鍵となります。

## 3. DB定義とマイグレーションの扱い（仕様に記述すべき内容と委譲すべき内容）

データベースのスキーマ変更やマイグレーションについては、「仕様書にどこまで記述し、どこからを実装側に任せるか」の線引きが重要です。原則として、仕様書には業務上必要なデータ要件や振る舞いのみを記述し、具体的なDBテーブル定義や移行手順は設計・実装に委譲します。

OpenSpecのベストプラクティスでは、データ要件は抽象レベルで仕様に反映し、詳細なDB変更は実装タスクに落とし込みます。例えば「二要素認証を追加する」仕様では、「ユーザーはログイン時に第二の認証要素を提供しなければならない」という要件とシナリオを仕様書に記載します。この中では「OTPシークレットを保存するためにDBカラムを追加する」等の直接的な技術語は書かれていません。一方で、その要件を実現するために必要なDB変更（「usersテーブルにOTPシークレット列を追加」「OTPログ用の新テーブルを作成」など）は、tasks.mdに実装手順として明記されます。以下に例を示します。

Spec記述例 (要求仕様): 「ユーザーはログイン時に第二の認証要素を提供しなければならない」

Tasks記述例 (実装手順): 「1.1 usersテーブルにOTPシークレット列を追加」「1.2 OTP検証ログ用のテーブルを作成」

このように、仕様書では「何を保存・管理すべきか」を述べ、そのための具体的なデータ構造変更（テーブル・カラム名や型など）は実装計画側に委ねるのが望ましいスタイルです。例えば、仕様書には「ユーザーのOTP秘密情報を安全に保持し、検証ログを記録すること」と書き、設計・タスク段階で「具体的にどのテーブルにどんなカラムを追加し、どのようなマイグレーションスクリプトを書くか」を扱います。

この責務分離により、仕様書はビジネスロジックや要件に集中し、DBの物理設計やマイグレーション手順といった実装寄りの情報で煩雑になるのを防げます。DBスキーマ変更はコードレビューやマイグレーションツールで管理すべき内容なので、OpenSpecでは**tasks.mdや必要に応じてdesign.mdで「DB変更タスク」や「移行戦略」を示し、仕様書自体は不変的な要件**の記述に留めるのがベストプラクティスです。

まとめると、仕様書には「どんなデータを扱う必要があるか」「そのデータが満たすべき要件（例: 一意性や必須性）」を記述し、テーブル構成やインデックス設計、移行スクリプトの作成手順などは実装側（設計/タスク）に委譲します。これにより、仕様段階では開発者・関係者全員が要件を共通理解しやすくなり、実装段階では詳細を詰める自由度を確保できます。

## 4. OpenSpec公式・コミュニティ事例からのベストプラクティスと失敗例

公式が提唱するベストプラクティス:
OpenSpec公式ドキュメントやガイドでは、いくつかの重要な指針が示されています。その一つが「Simplicity First（シンプルさ優先）」の哲学です。具体的には、「新規に生成されるコードはなるべく100行未満に抑える」「十分な理由がない限り複雑なフレームワークを導入しない」「検証済みで退屈なくらいシンプルなパターンを選ぶ」といったルールで、必要以上に凝った設計・実装を避けることが推奨されています。反対に、複雑さを追加してよい**「Complexity Triggers（複雑性を許容する条件）」**も定義されており、パフォーマンスデータで現行が明らかに遅い場合や、1000ユーザー以上といった具体的スケール要件がある場合など、根拠がある場合に限り抽象化や高度な仕組みの導入を検討します。このような基準を設けることで、常にシンプルで理解しやすい仕様・コードを保ち、必要時のみ段階的に複雑化することができます。

また、命名規則と構成についてのベストプラクティスも重要です。公式では、各**Capability（機能単位）の名前はできるだけ「動詞-名詞」**形式で表現し、一つのcapabilityが単一の目的に絞られるよう推奨しています（例: 認証機能ならuser-auth等）。説明が「～と～」とANDで繋がるようであれば機能を分割すべきサインです。同様に、変更IDも重複しないシンプルなkebab-caseで、add-やupdate-など動詞プレフィックス＋概要で表し、被りそうな場合は末尾に番号を付けてユニークにします。これにより、仕様や変更の目的がひと目で分かり、履歴管理もしやすくなります。

コミュニティからの知見と工夫:
OpenSpecユーザーコミュニティでも、多くの経験が共有されています。開発者たちは口を揃えて「実装コードのレビューに入る前に、まず仕様（Spec）のレビューと合意形成に注力すべき」と述べています。実際、OpenSpec開発者は「より良い結果を得るにはコードより仕様のブラッシュアップに時間を使うべきだ。コードを書き換えるより仕様を書き換える方がはるかに容易だ」と強調しています。この言葉通り、AIアシスタントを使った開発では、事前に詳細な仕様を詰めておくことで後工程の手戻りが減り、生産性と予測性が飛躍的に向上します。コミュニティ事例として、ある開発者はOpenSpec導入後に「AIが最初から正しい方向で実装を始めるようになり、プロンプトの試行錯誤や無駄な修正が激減した」と報告しています（従来は要求がチャット履歴に埋もれ、何度も意図を伝え直す必要があったが、仕様書で明確化したことで解決したとのこと）。

さらに、OpenSpecを使ったチーム開発では「変更内容の可視化と合意形成」が円滑になります。コミュニティの声として、「途中で仕様追加や変更に気付いても、OpenSpecなら一旦仕様を更新し直すことで全員（とAI）の認識を合わせ直せる」という利点が挙げられています。一方、OpenSpec導入前は開発中に新しい要求が出た際にタスク管理の混乱や手戻りが発生しがちでした。あるユーザはバックログ管理のみで進めていたところ、途中で機能追加を思いついて対応しようとした結果「プロジェクトがカオスになった」と述懐しています。OpenSpecではそうした場合でも、新たな変更提案を起こし仕様に盛り込むことで、計画と実装を再調整できます。もちろん予期せぬ変更が発生すれば多少の軌道修正は必要ですが、仕様駆動のプロセスを踏むことで影響範囲を把握しやすく、柔軟に対応可能です。

よくある失敗例と対策:
運用上の失敗例としてまず挙げられるのは、「仕様プロセスを飛ばしてしまう」ことです。例えば十分な仕様を書かずにAIに実装を開始させてしまうと、要求漏れや不要な機能実装が発生しがちで、結局後から大量のデバッグと仕様修正の往復になってしまいます。これはまさにOpenSpecが解決しようとしている問題であり、最初に仕様を固めることの大切さを裏付ける失敗パターンです。

逆に、仕様を書くことに固執しすぎて**「スペック駆動の麻痺」に陥るケースもあります。コミュニティには「詳細な仕様を書こうと半日費やしたがコードを書けず挫折した」という声もあり、最初から完璧を目指しすぎると生産性が落ちる教訓となっています。対策としては、仕様策定もイテレーティブに行い、ある程度の完成度で一旦実装に移してからまた仕様を磨くというサイクルを回すことです。OpenSpecは提案→レビュー→実装→アーカイブの循環を前提としているため、最初の提案はプロトタイピング**くらいの気持ちで書き、レビューを通して追記・修正していくぐらいが丁度良い運用になります。

他にも、「仕様と実装の同期ズレ」は失敗例として頻出します。例えば実装後に仕様書を更新し忘れたり、急ぎの修正でOpenSpecフローを省略してコードを直接変更した結果、ドキュメントと実態が食い違ってしまうケースです。これを防ぐには、必ず変更完了時にopenspec archiveを実行して仕様を反映・アーカイブすること、そして小さなバグ修正でも可能な限り後追いでいいので対応する仕様に注記を加えることが大切です。「仕様は常に最新」という意識をチーム全員が持ち、仕様とコードがワンセットで進化するよう統制することで、長期的な開発効率と品質を維持できます。

最後に、AIアシスタント任せでなく人間が仕様を精査する姿勢も忘れてはいけません。OpenSpecはAIが仕様書を生成・更新してくれますが、その内容の妥当性や抜け漏れのチェックは開発者の責務です。コミュニティでも「AI提案を鵜呑みにせず、自分で仕様をレビューすることで初めて真に安心して実装に移れる」との指摘があり、AIの出力を過信しないことも成功の秘訣とされています。要するに、OpenSpecはあくまで手段であり、最終的にプロジェクトを正しく導くのは人間の目と判断である点を肝に銘じておきましょう。

## 5. 運用ルールドラフト

OpenSpecを用いて仕様策定・管理を行う際の基本ルールを以下に箇条書きでまとめます。

仕様と設計・実装の分離: 仕様書（spec.md）には要求事項と振る舞いのみを記述し、技術的な方法論は設計書（design.md）またはタスク（tasks.md）に記載する。コードや詳細実装を仕様に書かないこと。

1機能1仕様主義: openspec/specs/直下には機能ごとにフォルダを分け、各機能の仕様はそれぞれ独立したspec.mdに整理する（ファイル名は機能を表す）。一つの仕様ファイルに複数の機能要件を詰め込まない。

横断的要件の扱い: 複数機能に影響する変更は、changes/<change-id>/specs/以下で各機能の差分を個別管理する。共通要件はproject.mdに記載し、各所への重複記述を避ける。

シナリオ駆動の記述: 全ての要求仕様には少なくとも一つのシナリオ（WHEN-THEN形式の受け入れ条件）を記載し、要件の意味を具体化する。シナリオが書けない要件定義は不十分とみなす。

データベース仕様の範囲: 仕様書には「扱うデータとその要件」のみを書き、テーブル名やカラム定義・マイグレーション手順は実装側に委ねる。DB変更はtasks.mdでタスク化し、必要なら設計書に補足する。

変更提案時の参照明示: 提案書（proposal.md）には変更の影響範囲として関連する仕様ファイル (specs/...) やソースディレクトリ (src/...) を明示し、どの部分に変更を加えるか関係者とAIにわかるようにする。

命名規則の遵守: change-idは短くわかりやすいkebab-case（例: add-user-auth）で一意に定める。Capability名も動詞-名詞形式で統一し、一目で目的が分かる名前にする。名前の一貫性は履歴管理・検索性を高める。

提案・承認プロセス: 新機能追加や仕様変更時は必ずOpenSpecの変更提案を作成し、チームでレビュー・合意してから実装に着手する。提案作成後はopenspec validate --strictでフォーマットチェックを行い、承認前に実装を始めない。

軽微な変更の扱い: バグ修正やリファクタリング等、動作を変えない軽微な変更は例外的に提案プロセスを省略してもよい。ただしその場合も必要に応じて関連仕様に注記し、仕様とコードの乖離を放置しない。

アーカイブの徹底: 変更実装完了後は必ずopenspec archive <change-id>を実行し、差分を正式な仕様に反映するとともに変更フォルダをarchive/へ移動する。これによりopenspec/specs/配下の仕様書が常に最新のシステム状態を示すよう維持する。

常に仕様を信頼源に: 「仕様書はプロダクトの真実」という意識を持ち、コードの変更は必ず対応する仕様の変更を伴わせること。仕様と実装を同期させ続けることで、AIアシスタントによる継続的な開発支援も正確さを保てる。

以上のルールを守ることで、OpenSpecを用いた開発プロセスが円滑に回り、チーム全体で予測可能かつ一貫性のある仕様運用が実現できます。各ルールはプロジェクトのWikiやopenspec/project.mdにも明記し、開発メンバー全員に周知徹底することを推奨します。
